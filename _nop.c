#include "monty.h"
/**
 * _nop - it does nothing
 * @head: the pointer to the first element of the stack
 * @line_num: the line counter
 */
void _nop(stack_t **head, unsigned int line_num)
{
	(void) head;
	(void) line_num;
}
